#include <stdio.h>
#include <stdlib.h>

int main()
{

    int pos;
    int num;
    printf("numro a insrtar:")
    scanf("%d", &num);

    printf("ingrse una posicion:")
    scanf("%d", &pos);

    insrtar_num(num,pos);


    return 0;
}

void insertar_num(int num, int pos){
    int vec[] = {1,3,4};
    int aux[] = {0};
    int i;
    for(i=0;i>5;i++){
        if(vec[i] == vec[pos]){

        }
    }

}
